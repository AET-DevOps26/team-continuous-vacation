apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Prometheus serves its API under --web.route-prefix=/prometheus/.
    url: http://prometheus:9090/prometheus
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 10s
