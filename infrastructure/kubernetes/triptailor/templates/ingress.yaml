{{- if .Values.ingress.enabled -}}
{{- if .Values.ingress.api.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "triptailor.fullname" . }}-api
  labels:
    app.kubernetes.io/component: gateway-api
    {{- include "triptailor.labels" . | nindent 4 }}
  {{- with .Values.ingress.api.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          - path: {{ $.Values.ingress.api.path }}
            pathType: {{ $.Values.ingress.api.pathType }}
            backend:
              service:
                name: {{ $.Values.backend.service.name }}
                port:
                  number: {{ $.Values.backend.service.port }}
    {{- end }}
---
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "triptailor.fullname" . }}
  labels:
    app.kubernetes.io/component: gateway-frontend
    {{- include "triptailor.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- if and $.Values.monitoring.enabled $.Values.monitoring.grafana.enabled $.Values.ingress.grafana.enabled }}
          - path: {{ $.Values.ingress.grafana.path }}
            pathType: {{ $.Values.ingress.grafana.pathType }}
            backend:
              service:
                name: {{ $.Values.monitoring.grafana.service.name }}
                port:
                  number: {{ $.Values.monitoring.grafana.service.port }}
          {{- end }}
          {{- if and $.Values.monitoring.enabled $.Values.ingress.prometheus.enabled }}
          - path: {{ $.Values.ingress.prometheus.path }}
            pathType: {{ $.Values.ingress.prometheus.pathType }}
            backend:
              service:
                name: prometheus
                port:
                  number: 9090
          {{- end }}
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $.Values.frontend.service.name }}
                port:
                  number: {{ $.Values.frontend.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
